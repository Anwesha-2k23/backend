# GCP Deployment - Visual Architecture & Flow

## System Architecture (Post-Deployment)

```
┌─────────────────────────────────────────────────────────────────┐
│                      FRONTEND (React/Vue)                        │
│              (your-frontend-domain.com)                          │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTPS Requests
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    GOOGLE CLOUD RUN                              │
│                  (Serverless Container)                          │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Django Backend (Python 3.8)                              │  │
│  │  - anwesha/settings.py (GCP config)                       │  │
│  │  - All your apps (user, event, atompay, etc.)             │  │
│  │  - Gunicorn (WSGI server)                                 │  │
│  │  - Auto-scaling: 1-10 instances                           │  │
│  │  - Port: 8080                                             │  │
│  │  - URL: anwesha-backend-XXXXX.run.app                    │  │
│  └────────┬─────────────────────┬──────────────────────┬────┘  │
│           │                     │                      │         │
│    Database Conn          Storage Conn          Email/External   │
└───────────┼─────────────────────┼──────────────────────┼────────┘
            │                     │                      │
    ┌───────▼──────────┐  ┌──────▼──────────┐  ┌───────▼─────────┐
    │  CLOUD SQL       │  │ CLOUD STORAGE   │  │ EXTERNAL APIS   │
    │  (MySQL 8.0)     │  │ (Static Files)  │  │                 │
    │                  │  │                 │  │ • Gmail (email) │
    │ Instance:        │  │ Bucket:         │  │ • Atom Pay      │
    │ anwesha-mysql    │  │ anwesha-        │  │ • Google OAuth  │
    │                  │  │ storage-bucket  │  │                 │
    │ Database:        │  │                 │  │                 │
    │ anwesha_db       │  │ Content:        │  │                 │
    │                  │  │ • /static/...   │  │                 │
    │ User:            │  │ • /profile/...  │  │                 │
    │ anwesha_user     │  │ • /qr/...       │  │                 │
    └──────────────────┘  │ • /gallery/...  │  └─────────────────┘
                          └─────────────────┘

```

## Request Flow - Event Registration with Payment

```
1. USER INITIATES REGISTRATION
   ┌──────────────────────────┐
   │ Frontend App             │
   │ (React/Vue)              │
   └────────────┬─────────────┘
                │ 1. User fills form + logs in
                │ 2. Gets JWT token (auth)
                │ 3. Clicks "Register & Pay"
                ▼
   ┌──────────────────────────────────────────────┐
   │ POST /atompay/ (with JWT cookie)             │
   │ {                                             │
   │   event_id: "1",                              │
   │   amount: "1000",                             │
   │   email: "user@example.com",                  │
   │   phone: "9876543210",                        │
   │   type: "solo" / "team"                       │
   │ }                                             │
   └────────────────┬─────────────────────────────┘
                    │
                    ▼ (CLOUD RUN)
   ┌──────────────────────────────────────────────┐
   │ Django Backend - payview()                   │
   │ • Authenticates JWT                          │
   │ • Encrypts payment data                       │
   │ • Calls Atom Auth API                        │
   └────────────────┬─────────────────────────────┘
                    │
                    ▼ (EXTERNAL)
   ┌──────────────────────────────────────────────┐
   │ Atom Payment API                             │
   │ https://payment1.atomtech.in/ots/aipay/auth │
   └────────────────┬─────────────────────────────┘
                    │
                    ▼ (CLOUD RUN)
   ┌──────────────────────────────────────────────┐
   │ Backend decrypts Atom response                │
   │ Returns atomTokenId (+ other data)            │
   │ ← Response to Frontend                        │
   └──────────────────────────────────────────────┘

2. USER COMPLETES PAYMENT
   ┌──────────────────────────┐
   │ Frontend submits form     │
   │ to Atom hosted checkout  │
   └────────────────┬─────────┘
                    │ POST to Atom endpoint
                    │ with atomTokenId
                    ▼
   ┌──────────────────────────────────────────────────┐
   │ Atom Payment Gateway (paynetzuat.atomtech.in)   │
   │ • Shows payment form                             │
   │ • User enters card details                       │
   │ • Processes payment                              │
   └────────────────┬─────────────────────────────────┘
                    │
                    ▼ (After payment success)
   ┌──────────────────────────────────────────────────┐
   │ Atom redirects to returnUrl                      │
   │ POST https://anwesha-backend-XXXXX.run.app/     │
   │     response/                                     │
   │ with encData (encrypted response)                │
   └────────────────┬─────────────────────────────────┘
                    │
                    ▼ (CLOUD RUN)
   ┌──────────────────────────────────────────────────┐
   │ Django Backend - resp()                          │
   │ • Decrypts encData                               │
   │ • Validates signature                            │
   │ • Creates SoloParticipants record (if payment ok)│
   │ • Updates Team.payment_done = True (if team)     │
   │ • Returns response.html                          │
   └────────────────┬─────────────────────────────────┘
                    │
                    ▼
   ┌──────────────────────────────────────────────────┐
   │ Payment confirmation page                        │
   │ shown to user with invoice details               │
   └──────────────────────────────────────────────────┘
```

## GCP Component Interaction

```
USER REQUEST
    │
    ▼
┌─────────────────────────────────────┐
│      CLOUD RUN (anwesha-backend)     │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  Gunicorn                   │   │
│  │  (4 workers)                │   │
│  └────────┬──────────┬─────────┘   │
│           │          │              │
│      Manages HTTP    Spawns         │
│      requests        Django         │
│                      instances      │
└─────────┬───────────────────────────┘
          │
          ├──────────────────────┬─────────────────┬───────────────┐
          │                      │                 │               │
    Query Data             Store Files          Send Email    External
    Processing            (Static, Media)       (Gmail API)   APIs
          │                      │                 │               │
          ▼                      ▼                 ▼               ▼
    ┌──────────────┐    ┌───────────────┐  ┌─────────────┐  ┌──────────┐
    │ CLOUD SQL    │    │ CLOUD STORAGE │  │   Gmail     │  │ Atom Pay │
    │              │    │               │  │             │  │          │
    │ MySQL 8.0    │    │ gs://anwesha- │  │ SMTP Server │  │ Payment  │
    │              │    │ storage-...   │  │             │  │ Gateway  │
    │ Port: 3306   │    │               │  │ Port: 587   │  │          │
    │              │    │ CDN Enabled   │  │             │  │ REST API │
    │ Replicated   │    │ (auto-serving │  │ TLS         │  │          │
    │ (automatic)  │    │  files)       │  │             │  │          │
    └──────────────┘    └───────────────┘  └─────────────┘  └──────────┘
```

## Data Flow - File Upload Example

```
USER UPLOADS PROFILE PICTURE

1. Frontend sends POST request with file
   ▼
2. Cloud Run receives and processes
   ▼
3. File is saved to Cloud Storage
   gs://anwesha-storage-bucket/static/profile/...
   ▼
4. Cloud Storage returns public URL
   ▼
5. URL stored in database (Cloud SQL)
   ▼
6. When frontend requests user profile
   ▼
7. Backend returns URL: https://storage.googleapis.com/
                        anwesha-storage-bucket/static/profile/...
   ▼
8. Browser downloads file directly from Cloud Storage CDN
   (fast, global distribution)
```

## Environment Configuration Flow

```
┌──────────────────────────────────────────────────┐
│          You prepare .env file locally            │
│                                                  │
│  SECRET_KEY=xxxxx                                │
│  DB_PASSWORD=xxxxx                               │
│  ALLOWED_HOSTS=cloud-run-domain.run.app          │
│  etc...                                          │
└──────────────────────────┬───────────────────────┘
                           │
                           ▼
           ┌───────────────────────────────┐
           │  Include in deploy command    │
           │  OR in Cloud Run secrets      │
           └──────────────┬────────────────┘
                          │
                          ▼
        ┌──────────────────────────────────┐
        │   Cloud Run Container at startup │
        │   reads .env / environment vars  │
        └──────────────┬───────────────────┘
                       │
                       ▼
        ┌──────────────────────────────────┐
        │  Django settings.py loads them   │
        │  via env() function              │
        │  (python-environ)                │
        └──────────────┬───────────────────┘
                       │
                       ▼
        ┌──────────────────────────────────┐
        │  Application uses values         │
        │  • Connects to Cloud SQL         │
        │  • Connects to Cloud Storage     │
        │  • Sends emails via Gmail       │
        │  • Accepts requests from CORS   │
        └──────────────────────────────────┘
```

## Scaling Behavior

```
TRAFFIC PATTERNS

Low Traffic (1-2 users)
├─ 1 Cloud Run instance running
├─ CPU usage low
├─ Very low cost (~$0.01/hour)
└─ Response time: <100ms

Medium Traffic (10-100 users)
├─ Auto-scale to 3-5 instances
├─ Requests distributed via load balancer
├─ CPU usage moderate
├─ Cost: ~$0.05-0.10/hour
└─ Response time: <100ms (load balanced)

High Traffic (100+ concurrent users)
├─ Auto-scale to max 10 instances
├─ All instances active
├─ CPU usage high
├─ Cloud SQL might need upgrade
├─ Cost: ~$0.20-0.50/hour
└─ Response time: <200ms

Scaling Rules:
├─ Min instances: 1 (always running, saves cold start time)
├─ Max instances: 10 (prevents runaway costs)
├─ CPU target: 65% (scale when CPU > 65%)
└─ Cool down: 60 seconds between scaling events
```

## Cost Breakdown (Monthly Estimate)

```
┌─────────────────────────────────────────────┐
│  CLOUD RUN COSTS                            │
│  ─────────────────────────────────────────  │
│  • vCPU: $0.00002400 per second             │
│  • Memory: $0.00000250 per GB-second        │
│  • Requests: $0.40 per million              │
│  (1 instance running 24/7 ≈ $5-10)         │
├─────────────────────────────────────────────┤
│  CLOUD SQL COSTS (db-f1-micro)              │
│  ─────────────────────────────────────────  │
│  • Instance: $9.00                          │
│  • Storage: $0.18 per GB (10GB ≈ $1.80)    │
│  • Backup: included                         │
│  (Total ≈ $11/month)                        │
├─────────────────────────────────────────────┤
│  CLOUD STORAGE COSTS                        │
│  ─────────────────────────────────────────  │
│  • Storage: $0.020 per GB                   │
│  • Operations: low                          │
│  • Egress: $0.12 per GB (if >1GB/month)    │
│  (10GB files ≈ $0.20/month)                │
├─────────────────────────────────────────────┤
│  NETWORKING COSTS                           │
│  ─────────────────────────────────────────  │
│  • Ingress: Free                            │
│  • Egress (to internet): $0.12/GB           │
│  (Low for API, high for large downloads)   │
├─────────────────────────────────────────────┤
│  ESTIMATED TOTAL (Low Traffic)              │
│  ─────────────────────────────────────────  │
│  • Per month: $15-25                        │
│  • Per year: $180-300                       │
└─────────────────────────────────────────────┘
```

## File Organization

```
d:\Anwesha\backend\
│
├── anwesha/                    (Django project)
│   ├── settings.py             [MODIFIED - GCP config]
│   ├── storage_backend.py       [MODIFIED - GCS support]
│   ├── asgi.py
│   ├── wsgi.py
│   └── ...
│
├── user/
│   ├── views.py
│   ├── models.py
│   └── ...
│
├── event/
├── atompay/
├── sponsor/
├── templates/
├── static/
├── media/
│
├── Dockerfile                  [MODIFIED - Cloud Run ready]
├── requirements.txt            [MODIFIED - Added GCP deps]
├── docker-compose.yml
│
├── .env.example                [NEW - Environment template]
├── app.yaml                    [NEW - App Engine config]
│
├── GCP_DEPLOYMENT.md           [NEW - Step-by-step guide]
├── GCP_DEPLOYMENT_CHECKLIST.md [NEW - Pre-flight checklist]
├── GCP_DEPLOYMENT_SUMMARY.md   [NEW - Overview of all changes]
├── GCP_VALUES_REFERENCE.md     [NEW - Value tracker]
├── PAYMENT_CONFIG_FOR_GCP.md   [NEW - Payment setup]
├── deploy-to-gcp.sh            [NEW - Auto-deploy script]
│
└── manage.py
```

---

**You're all set!** 🚀 Follow the steps in `GCP_DEPLOYMENT.md` to deploy your backend to production.
